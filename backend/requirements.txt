# FastAPI and web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Data validation
pydantic==2.5.0
pydantic-settings==2.1.0

# HTTP client (used by embeddings service and github_ingestion)
httpx>=0.27.0

# Configuration
python-dotenv==1.0.0

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Utilities
numpy==1.26.2

# Production features
tenacity==8.2.3          # Retry logic with exponential backoff
prometheus-client==0.19.0  # Metrics and monitoring
esprima==4.0.1           # JavaScript AST parsing for smart chunking

# Cloud services (replacing local stack)
qdrant-client>=1.7.0,<2.0.0  # Qdrant Cloud vector database
groq>=0.4.0              # Groq API (LLM inference)
upstash-redis>=1.0.0     # Upstash Redis (serverless HTTP cache)
gitpython>=3.1.40        # GitHub repo cloning
